<?xml version="1.0" encoding="UTF-8"?>
<!-- A Maven Surefire consumer of the qe-report JUnit Platform adapter, resolved as a published
     artifact from the build-local repository. Exists only to prove ServiceLoader discovery and
     forked execution; Maven is not part of the repository's own build. -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>io.github.yannisyoussef.fixtures</groupId>
  <artifactId>qe-report-maven-consumer</artifactId>
  <version>0.1.0</version>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <junit.version>6.1.3</junit.version>
    <qe.localRepo>file:///unset</qe.localRepo>
    <qe.report.dir>target/qe-report</qe.report.dir>
    <qe.report.runId>run-maven-consumer</qe.report.runId>
  </properties>

  <repositories>
    <repository>
      <id>qe-build-local</id>
      <url>${qe.localRepo}</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.github.yannisyoussef</groupId>
      <artifactId>qe-report-junit-platform</artifactId>
      <version>0.1.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.6.0</version>
        <configuration>
          <!-- One JVM per test class, three at a time. -->
          <forkCount>3</forkCount>
          <reuseForks>false</reuseForks>
          <testFailureIgnore>true</testFailureIgnore>
          <systemPropertyVariables>
            <qe.report.dir>${qe.report.dir}</qe.report.dir>
            <qe.report.runId>${qe.report.runId}</qe.report.runId>
          </systemPropertyVariables>
          <properties>
            <configurationParameters>
              junit.jupiter.execution.parallel.enabled = true
              junit.jupiter.execution.parallel.mode.default = concurrent
            </configurationParameters>
          </properties>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
